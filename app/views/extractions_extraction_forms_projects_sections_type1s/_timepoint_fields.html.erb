<tr class="nested-fields timepoint-nested-fields" removed="false">
  <%= f.simple_fields_for(:timepoint_name, f.object.timepoint_name || f.object.build_timepoint_name) do |tn_form| %>
    <td class="remove-tp-link" style="font-size: x-large;">
      <%= link_to_remove_association '✖', f %>
    </td>
    <!-- /td.field = f.check_box :_destroy -->
    <td class="tp-name-input field">
      <%= tn_form.input :name, label: 'Number', required: true %>
    </td>
    <td class="tp-unit-input field">
      <%= tn_form.input :unit, label: 'Timepoint unit (e.g., wk)' %>
    </td>
  <% end %>
</tr>

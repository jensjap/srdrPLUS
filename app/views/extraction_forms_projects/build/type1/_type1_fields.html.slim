.nested-fields
  = f.input :name, required: true, hint: 'Name can\'t be blank', autofocus: true
  = f.input :description, input_html: { rows: 4 }

  = link_to_remove_association "Remove #{ efps.section.name.titleize.singularize } Suggestion", f

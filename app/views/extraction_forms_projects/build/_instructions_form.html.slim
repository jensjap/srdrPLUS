- section_name =  efps.section.name.titleize.singularize

h4 Instructions for this Section (#{ section_name }s)
blockquote
  ' You may provide instructions specific to this #{ section_name }s section for extractors (maximum of approximately 1024 characters including whitespace).
    These instructions will be visible during data extraction but will not appear in the exported data.
  cite Friendly Helper

= simple_form_for efps, remote: true, html: { id: 'efps-instructions-form', class: 'pb-5' } do |f|
  = f.input :instructions, label: 'Please enter the instruction text below:', input_html: { rows: 4, maxlength: 1024 }
  = f.button :submit, 'Save Instructions', class: 'btn btn-primary', id: 'save-instructions-btn'

javascript:
  $(document).ready(function() {
    $('#efps-instructions-form').on('ajax:beforeSend', function() {
      $('#save-instructions-btn').prop('disabled', true);
    });

    $('#efps-instructions-form').on('ajax:success', function() {
      $('#save-instructions-btn').prop('disabled', false);
    });
  });

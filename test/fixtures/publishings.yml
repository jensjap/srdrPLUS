# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  publishable: one (Project)
  user: one
  active: true

two:
  publishable: two (Project)
  user: one
  active: true

